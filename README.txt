Patch: Startup config logging

This patch adds two log lines at startup:
 - which settings.json file is used
 - which journal_dir is active

Overwrite main.py with the patched version provided previously.