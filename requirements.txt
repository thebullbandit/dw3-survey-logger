# DW3 Survey Logger - runtime dependencies
# Stdlib modules (tkinter, sqlite3, etc.) are intentionally not listed.

PyYAML>=6.0
pynput>=1.7.6

# Windows-only (used for optional window focus helpers)
pywin32>=306; platform_system=="Windows"
